<div xmlns="http://www.w3.org/1999/xhtml"><?xml-stylesheet href="../common.css"?>
<div class="navbar"><a href="Scripting.xml">Scripting</a> &gt; Entities</div>
<p>Entities are the main part of Mokoi Games as they control everything. Currently Entities are written in <a href="Scripting;Pawn.xml">Pawn</a> but other languages may be added down the track.</p>
<h2>Entity Entry Points</h2>

<h3>main()</h3>

The 'main' function is the main entry point to the entity. This is called each frame.
<h3>Init(...)</h3>

The 'Init' function is called on the entity when created. For local/map entities this is also called each loading of the map.
<h3>Close()</h3>

The 'Close' function is called on the entity when deleted. For local/map entities this is also called each closing of the map.
<h3>UpdatePosition()</h3>

<p>The 'UpdatePosition' function is called on Global entities when switching maps. The main use for these to change the Display Objects.</p>
<h2>Main Entity Entry Points</h2>

<p>The main entity is required for games to work. The main entity has a couple of entry points to allow scripting enhancements.</p>
<h3>save()</h3>

Called before the game state is saved.
<h3>load()</h3>

Called before the game state is loaded.
<h3>dialogbox(line)</h3>

<p>This function is called then DialogShow, so you can have a custom dialog box.</p>
<h2>Custom Entry Points</h2>

<h3>KeyboardInput(unicode)</h3>

<p>When KeyboardWatch is used in a entity, all keyboard input is sent to this function.</p>


</div>